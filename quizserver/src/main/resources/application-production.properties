# Production Profile for Render.com PostgreSQL
# Completely override any datasource configuration from application.properties
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://quiz_admin:XoZI5UJG5BoB2pmgtk5HsHzalb9nlv7u@dpg-d231veje5dus73a5nd5g-a.oregon-postgres.render.com/quiz_beta_db}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.sql.init.continue-on-error=true
spring.jpa.defer-datasource-initialization=true

# Application specific properties
spring.application.name=QuizBeta
server.port=${PORT:10000}

# JPA Configuration for PostgreSQL
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:quizBetaSecretKeyForJWTTokenGeneration2025QuizApplication}
jwt.expiration=86400000

# Actuator for health checks
management.endpoints.web.exposure.include=health
